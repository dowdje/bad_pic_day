<%= form_for(photo) do |f| %>
  <% if photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(photo.errors.count, "error") %> prohibited this photo from being saved:</h2>

      <ul>
      <% photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :caption %>
    <%= f.text_field :caption %>
  </div>

  <div class="field">
  <%= f.fields_for :celebrity, photo.celebrity do |celeb| %>
  <%= celeb.label "Who is it?" %>
  <%= celeb.text_field :name %>
  <% end %>

  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
