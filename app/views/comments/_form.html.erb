<%= form_for @comment do |f| %>
<%= f.label :content %>

<%= f.text_field :content %>
<%= f.hidden_field :photo_id, value: @comment.photo.id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
